[build]
  command = "pip install -r requirements.txt"
  publish = "."
 
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
 
[[redirects]]
  from = "/generate"
  to = "/.netlify/functions/app/generate"
  status = 200